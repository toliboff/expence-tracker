<h1>TRANSACTIONS</h1>

<ul>
  <% @groups.each do |group|%>
    <li>
      <img src=''>
      <div>
        <h5><%= group.name %></h5>
        <span><%= group.created_at%><span>
      </div>
      <span>$<%= group.amount%></span>
    </li>
  <% end %>
</ul>